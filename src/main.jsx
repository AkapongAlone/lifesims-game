import React from "react";
import ReactDOM from "react-dom/client";
import { ModernApp } from "./redesign/index.js";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ModernApp />
  </React.StrictMode>
);
